# stats-engine (PR1, 이변량은 PR3-A). 정확한 버전 고정(==) — 오프라인 wheel 캐시
# 관리 시 이 버전을 기준으로 wheel을 받는다. 버전을 올릴 때는
# test_descriptive.py/test_bivariate.py의 R 참조값 비교(rtol/atol)가 여전히
# 통과하는지 확인할 것. scipy==1.13.0엔 Welch ANOVA API가 없어 bivariate.py의
# anova()가 공식을 직접 구현한다 — 버전을 올려도 이 구현은 그대로 유지한다(불필요한
# scipy API 의존을 새로 만들지 않기 위함).
numpy==1.26.4
scipy==1.13.0
jsonschema==4.22.0
# PR4-A1 — 연관성 회귀(OLS·이분 로지스틱). statsmodels는 로지스틱 계수 추정(MLE)과
# 적합도 지표(logLik/aic/pseudoR2)에만 쓴다 — covariance(HC3/person-cluster CR1)는
# regression.py가 직접 계산한다. 이유: statsmodels 0.14.2의 Logit은
# cov_type='HC3'를 받아도 결과 클래스에 cov_HC3가 없어 cov_white_simple
# (use_correction=False)로 대체되고 실제로는 HC0를 계산한다 — "HC3" 라벨로 HC0
# 값을 내보내면 소표본(이 시스템은 673 사례 규모)에서 p값이 실제보다 작게 나온다.
# OLS는 statsmodels의 HC3도 정상 동작하므로 자체 계산값과의 교차검증에만 쓴다.
# pandas·patsy가 statsmodels의 전이 의존으로 함께 설치된다(직접 사용하지 않음).
statsmodels==0.14.2
