# Build artifacts (rebuilt inside Docker)
dist/
node_modules/
server/node_modules/
shared/node_modules/
packages/analytics-core/node_modules/
packages/analytics-core/dist/

# VCS
.git/
.gitignore

# Electron (not needed for server image)
electron/
out/
release/

# Dev / local files
.env
.env.*
*.local
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.code-workspace

# Test / coverage
coverage/
.nyc_output/

# Claude / review tooling
.claude/
.code-review-graph/

# pose-inference dev artifacts (6.0-9) — venv/캐시/샘플영상/산출물 제외. models/*.onnx(baked 가중치)는 포함.
# pose-inference/.gitignore의 privacy guard(§8.13)와 정합: out/(산출 keypoints는 실영상 파생 가능 → 반출 금지),
# samples/·영상파일(재현 불가), _*.py(로컬 probe)는 이미지에 굽지 않는다(검증은 운영자 클립 마운트).
services/pose-inference/.venv/
services/pose-inference/samples/
services/pose-inference/out/
services/pose-inference/_*.py
services/pose-inference/**/*.mp4
services/pose-inference/**/*.mov
services/pose-inference/**/*.avi
services/pose-inference/**/*.mkv
services/pose-inference/**/*.webm
**/__pycache__/

# PR1 stats-engine dev artifacts — venv/캐시는 이미지에 굽지 않는다(요구사항은
# Dockerfile이 별도로 pip install한다). fixtures/(R 참조값·pytest 전용)는 런타임에
# 불필요하지만 크기가 무의미해 제외하지 않는다.
services/stats-engine/.venv/
