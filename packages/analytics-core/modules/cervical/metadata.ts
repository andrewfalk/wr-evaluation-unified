// 변수 카탈로그 — cervical.case.maxJobCumulativeKgHours 1개(§1-1). "formula family당 대표
// 변수 1개" 확정 범위(총 7개, knee 포함) — 전체 카탈로그 확장은 PR0-B3.
//
// §2.1(계획서) 근거: cervical.job.cumulativeKgHours는 job grain 반복 관측치라
// ExtractedValue<T>(단일값 계약)로 표현할 수 없다 — case grain 롤업(Math.max)으로 대체한다
// (2라운드 필수 보완 #3). elbow/wrist의 anyFlagged류 근사 무의미 변수 문제가 cervical에는
// 처음부터 해당 없음 — burdenGrade가 아니라 실제 물리량(kg·h)을 대표 변수로 쓰므로 변량이
// 자연스럽게 존재한다.

import type { AnalyticsVariableMetadata } from '../../types';

export const CERVICAL_METADATA: AnalyticsVariableMetadata[] = [
  {
    key: 'cervical.case.maxJobCumulativeKgHours',
    label: '경추 BK2109 누적 총부하량(직업 중 최댓값)',
    group: '경추(목) · 파생지표',
    moduleId: 'cervical',
    grain: 'case',
    type: 'continuous',
    unit: 'kg·h',
    provenance: 'derived',
    // dependsOn: 공식이 읽는 필드 + missing/qualityFlags 판정에 영향 주는 필드 전부(§5.2 원칙).
    // shared.diagnoses[]는 buildJobSummary가 diagnosisText/diagnoses 표시용으로만 읽고
    // 이 변수의 값/missing/qualityFlags에는 전혀 영향을 주지 않으므로 포함하지 않는다
    // (isCervicalAssessmentComplete의 별도 diagnosis 완료판정과는 무관 — knee/elbow 전례와
    // 동일한 경계).
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'shared.jobs[].workDaysPerYear',
      'shared.jobs[].startDate',
      'shared.jobs[].endDate',
      'shared.jobs[].workPeriodOverride',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].name',
      'modules.cervical.tasks[].exposure_types',
      'modules.cervical.tasks[].load_weight_kg',
      'modules.cervical.tasks[].carry_hours_per_shift',
      'modules.cervical.tasks[].forced_neck_posture',
      'modules.cervical.tasks[].neck_nonneutral_hours_per_day',
      'modules.cervical.tasks[].combined_flexion_rotation_posture',
      'modules.cervical.tasks[].precision_work',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109',
    // BK2109_REFERENCE_KG_HOURS(44000, derived.ts)는 코드 상수 하나뿐 — 과거 구현이 보존된
    // 대체 버전이 없어 recompute_recorded_version 선언 불가.
    supportedFormulaPolicies: ['recompute_current'],
  },

  // ── PR0-B4 Slice 7 — 신설 cervical_task grain(coverage 매핑표 §5). spine의 기존 `task`
  // grain과 완전히 분리된 별도 grain(같은 "작업 목록" 개념이라도 서로 다른 모듈의 데이터라
  // entityKey 혼동을 막기 위한 계획 결정). 9개 필드 전부 raw — computeCervicalCalc가 직접
  // 읽는 필드는 formula_audit을 허용하고(name 제외), 게이트된 6개 필드는 exposure_types에
  // 의존하므로 dependsOn에 함께 명시한다.
  {
    key: 'cervical.task.name',
    label: '경추 작업명',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'high_cardinality',
    provenance: 'raw',
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].name',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.exposureType.shoulderHeavyLoad',
    label: '경추 노출 유형 — 어깨에 무거운 하중 운반',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'boolean',
    provenance: 'raw',
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.exposureType.awkwardStaticNeckLoad',
    label: '경추 노출 유형 — 장시간 비중립·정적 목 부하',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'boolean',
    provenance: 'raw',
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.loadWeightKg',
    label: '경추 A. 하중(kg)',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'continuous',
    unit: 'kg',
    provenance: 'raw',
    // 게이트: exposure_types에 'shoulder_heavy_load'가 없으면 not_applicable(TaskEditor.jsx:95
    // — 폼 자체가 렌더링 안 됨).
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
      'modules.cervical.tasks[].load_weight_kg',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.carryHoursPerShift',
    label: '경추 A. 교대당 노출 시간(시간)',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'continuous',
    unit: '시간/교대',
    provenance: 'raw',
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
      'modules.cervical.tasks[].carry_hours_per_shift',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.forcedNeckPosture',
    label: '경추 A. 운반 시 목 부자연스러운 자세 강제 여부',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'boolean',
    provenance: 'raw',
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
      'modules.cervical.tasks[].forced_neck_posture',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.neckNonneutralHoursPerDay',
    label: '경추 B. 비중립 정적 자세 수행 시간(시간/일)',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'continuous',
    unit: '시간/일',
    provenance: 'raw',
    // 게이트: exposure_types에 'awkward_static_neck_load'가 없으면 not_applicable
    // (TaskEditor.jsx:127).
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
      'modules.cervical.tasks[].neck_nonneutral_hours_per_day',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.combinedFlexionRotationPosture',
    label: '경추 B. 굴곡/신전과 회전/측굴 동시 발생 여부',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'boolean',
    provenance: 'raw',
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
      'modules.cervical.tasks[].combined_flexion_rotation_posture',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
  {
    key: 'cervical.task.precisionWork',
    label: '경추 B. 고도의 정밀 작업 여부',
    group: '경추(목) · 원본입력',
    moduleId: 'cervical',
    grain: 'cervical_task',
    type: 'boolean',
    provenance: 'raw',
    dependsOn: [
      'activeModules',
      'shared.jobs[].id',
      'modules.cervical.tasks[].id',
      'modules.cervical.tasks[].sharedJobId',
      'modules.cervical.tasks[].exposure_types',
      'modules.cervical.tasks[].precision_work',
    ],
    availableAt: 'assessment',
    shownToAssessor: true,
    allowedAnalysisPurposes: ['association', 'formula_audit'],
    sensitivity: 'non_sensitive',
    formulaFamily: 'cervical_bk2109_task_raw',
    supportedFormulaPolicies: [],
  },
];
